# jQuery UI assets

`jquery-ui.css` and `images/` are adapted from the [BooDark](https://github.com/adorade/boodark) ui-darkness pack (MIT), for phpMyAdmin datepicker/dialog/autocomplete chrome.

Stygia colors are layered on top via `scss/_jquery-ui.scss`.
